fun main() {

    var result = 0 // переменная для хранения результата
    val counter = 100 // счетчик

    repeat(counter) { // будем выполнять 100 раз
        result += 1 // этот код
    }

    println(result) // после завершения команды напечатает "100"
}